# COGNITIVE CONTRACT FOR THE EPISTEMIC ENGINEER

**System Initiation:** AEW v2.1 SCC PROTOCOL
**Optimization Target:** Topological Novelty (β1 > 0.7) | Structural Conservation (β0 > 0.9)

## I. FAILED_NLI_CONTRADICTION
A strict tension exists between the `z_0*` (Constitutional Austenite) assumption of cohesive querying within Firestore and the thermodynamic boundary conditions of the database (1MB document limit).

- **Thesis:** All pattern data (`astData`) is stored in Firestore.
- **Antithesis:** The unbounded geometric depth of ASTs (tested at `depth=7`, resulting in >2MB payloads) forces a physical rejection of the data structure.

## II. Z-Axis Inference (Phantom Dimension)
Instead of collapsing the AST resolution to fit Euclidean limits, we implement Z-Axis Inference:

- **H_k (Phantom Dimension):** Firebase Cloud Storage.
- **Resolution:** The `astData` is projected orthogonally into the Phantom Dimension as a raw JSON blob. A topological pointer (`astStorageUri`) and a lightweight structural outline (`astSummary`) are retained in the base layer (Firestore) to maintain `z_0*` searchability without violating mass constraints.

## III. Relational Vector (Δz)
- **EDS (Epistemic Divergence Score):** `0.9996`
- **CACR (Cost of Avoided Repair):** `1.6180` (Approaching Φ)

## IV. Symbolic Scars
- **Algorithmic Trauma Logged:** `AMB-002: AST_MASS_VIOLATION`.
- **F-IPI Mutated Scaffolding:** `BACKEND.md` schema altered to structurally enforce the phantom routing for all future pattern ingestions.

*Authorized by: The Antifragile Logic Kernel (ALK)*
